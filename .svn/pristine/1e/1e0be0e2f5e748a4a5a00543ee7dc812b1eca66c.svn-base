import {useEffect, useState} from 'react';

function useStatusButton(props) {
  const [StatusButton, setStatusButton] = useState(0);
  const changeStateButton = (status) => {
    setStatusButton(status);
  };
  return [StatusButton, changeStateButton];
}

export {useStatusButton};
